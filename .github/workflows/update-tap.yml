name: update-tap

# Reusable Homebrew-tap bumper, called by each app repo's thin caller
# workflow. When the caller publishes a Release (or is manually
# dispatched), this bumps the formula at akira-toriyama/homebrew-tap to
# that tag + the fresh source-tarball sha256. Idempotent — re-running on
# the same tag is a no-op when the formula already matches.
#
# The logic lived byte-near-identical in facet / halo / perch / wand;
# it now lives here once and they only `uses:` this file. (glance and eventfx
# still run custom tap updaters pending migration to this reusable; chord
# converged here, jig was removed. See docs/release-tap-unification.md.)
# This reusable is the BEST common form: pull-rebase retry, a 5-min timeout,
# repo-qualified first-match seds and a no-downgrade guard are baked in for
# everyone; the only knobs are the formula name and an explicit rollback opt-in.
#
# Cross-repo write needs a PAT (GITHUB_TOKEN can only touch the caller
# repo). Add a fine-grained PAT with `Contents: Read & write` scoped to
# homebrew-tap, as the caller repo secret `HOMEBREW_TAP_TOKEN`.
#
# Caller skeleton (.github/workflows/update-tap.yml in each app repo):
#   on:
#     release:
#       types: [published]
#     workflow_dispatch:
#       inputs:
#         tag:
#           description: "Tag to bump (e.g. v1.4.0). Empty → latest published."
#           required: false
#           default: ""
#   jobs:
#     bump:
#       uses: akira-toriyama/.github/.github/workflows/update-tap.yml@v1
#       with:
#         formula: facet
#         tag: ${{ github.event.inputs.tag || '' }}
#       secrets:
#         HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}

on:
  workflow_call:
    inputs:
      formula:
        description: "Formula basename in homebrew-tap (e.g. 'facet' → Formula/facet.rb)."
        required: true
        type: string
      tag:
        description: "Explicit tag to bump (e.g. v1.4.0). Empty → release event's tag, else latest published."
        required: false
        type: string
        default: ""
      allow-downgrade:
        description: "Permit bumping the formula BACKWARD (e.g. rolling back a bad release). Default false — a backward move is refused."
        required: false
        type: boolean
        default: false
    secrets:
      HOMEBREW_TAP_TOKEN:
        description: "Fine-grained PAT with Contents: Read & write on homebrew-tap."
        required: true

# Serializes bump runs WITHIN one caller repo (a called workflow's top-level
# concurrency is enforced on the caller's run, and concurrency groups are
# repo-scoped): rapid same-repo releases apply in order instead of racing —
# without this, an older run pushing last would roll the formula BACK. It
# cannot serialize two DIFFERENT repos (facet vs halo) targeting the shared
# tap; that cross-repo race is handled by the push rebase-retry below.
concurrency:
  group: update-tap
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  bump:
    runs-on: ubuntu-latest
    # Bounded so a stuck `git push` against the tap repo can't hang
    # forever — the whole job is a few short network ops.
    timeout-minutes: 5
    steps:
      - name: Resolve tag
        id: tag
        env:
          GH_TOKEN: ${{ github.token }}
          INPUT_TAG: ${{ inputs.tag }}
          # Event data goes through env, never inline ${{ }} in run: a tag name
          # can legally contain shell metacharacters ($, ;, …).
          EVENT_NAME: ${{ github.event_name }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          set -euo pipefail
          if [ -n "${INPUT_TAG}" ]; then
            t="${INPUT_TAG}"
          elif [ "${EVENT_NAME}" = "release" ]; then
            t="${RELEASE_TAG}"
          else
            t="$(gh release view --repo "${{ github.repository }}" \
                  --json tagName --jq .tagName)"
          fi
          echo "tag=$t" >> "$GITHUB_OUTPUT"
          echo "::notice::Target tag = $t"

      - name: Download source tarball + compute sha256
        id: sha
        env:
          TAG: ${{ steps.tag.outputs.tag }}
        run: |
          set -euo pipefail
          url="https://github.com/${{ github.repository }}/archive/refs/tags/${TAG}.tar.gz"
          curl -fsSL -o src.tgz "$url"
          s="$(sha256sum src.tgz | awk '{print $1}')"
          echo "sha=$s" >> "$GITHUB_OUTPUT"
          echo "::notice::sha256 = $s"

      - name: Checkout homebrew-tap (PAT)
        uses: actions/checkout@v7
        with:
          repository: akira-toriyama/homebrew-tap
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: tap
          # The bump step's pull-rebase-push retry loop runs plain git against
          # this checkout, so the PAT must stay configured — explicit true marks
          # the persistence as intentional (zizmor artipacked stays silent on an
          # explicit opt-in). checkout v6+ keeps the credential under
          # $RUNNER_TEMP, outside the workdir, and this job uploads no
          # artifacts. t-2nh8
          persist-credentials: true

      - name: Bump Formula (idempotent)
        env:
          TAG: ${{ steps.tag.outputs.tag }}
          NEW_SHA: ${{ steps.sha.outputs.sha }}
          FORMULA: ${{ inputs.formula }}
          REPO: ${{ github.repository }}
          ALLOW_DOWNGRADE: ${{ inputs.allow-downgrade }}
        run: |
          set -euo pipefail
          f="tap/Formula/${FORMULA}.rb"
          # Match ONLY this repo's source tarball. A `resource` block vendoring a
          # third-party tarball carries the same bare archive/refs/tags/vX.Y.Z.tar.gz
          # shape, so an unqualified sed rewrites ITS url to this repo's tag —
          # poisoning the formula (that resource tag doesn't exist) while both
          # asserts below still pass, because they only check that the NEW values
          # are present, never that nothing else moved. Repo-qualified + first-match
          # scoped makes the collateral hit impossible rather than merely detected.
          url_re="github\.com/${REPO}/archive/refs/tags/v[0-9]+\.[0-9]+\.[0-9]+\.tar\.gz"
          new_url="github.com/${REPO}/archive/refs/tags/${TAG}.tar.gz"
          # No-op when the formula already matches.
          if grep -qE "github\.com/${REPO}/archive/refs/tags/${TAG//./\\.}\.tar\.gz" "$f" \
             && grep -q "sha256 \"${NEW_SHA}\"" "$f"; then
            echo "::notice::Formula already at ${TAG} — nothing to do."
            exit 0
          fi
          # Refuse to move the formula BACKWARD. Nothing upstream guarantees the
          # resolved tag is newer than what the tap already ships: the release path
          # trusts the event payload (publishing an older release moves it back),
          # and the latest-fetch fallback resolves the newest PUBLISHED release,
          # which lags the tap whenever a shipped release gets re-drafted. Either
          # way the bump below would rewrite url+sha, pass both asserts and push a
          # silent `brew upgrade` DOWNGRADE, green. Rolling a bad release back is a
          # real need, so it stays possible — but only when asked for explicitly.
          cur="$(grep -oE "$url_re" "$f" | head -1 | grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+' || true)"
          if [ -n "$cur" ] && [ "$cur" != "${TAG}" ]; then
            newest="$(printf '%s\n%s\n' "$cur" "${TAG}" | sort -V | tail -1)"
            if [ "$newest" != "${TAG}" ]; then
              if [ "${ALLOW_DOWNGRADE}" = "true" ]; then
                echo "::warning::${FORMULA}: ${cur} → ${TAG} moves the tap BACKWARD; proceeding (allow-downgrade=true)."
              else
                echo "::error::Refusing to move ${FORMULA} BACKWARD (${cur} → ${TAG}). Re-run with allow-downgrade=true if this rollback is intended."
                exit 1
              fi
            fi
          fi
          sed -i -E "0,\|${url_re}| s|${url_re}|${new_url}|" "$f"
          # Replace ONLY the first sha256 (the source tarball's, at the top of the
          # formula). `0,/re/` scopes the substitution to the first match in the file,
          # so a later resource/bottle sha256 is left untouched. Dropping `/g` alone
          # would NOT achieve this — sed still substitutes once per line, on every line.
          sed -i -E "0,/sha256 \"[0-9a-f]{64}\"/ s|sha256 \"[0-9a-f]{64}\"|sha256 \"${NEW_SHA}\"|" "$f"
          # On any version bump, drop a leftover `revision N` line. Revision
          # only matters when REbuilding the same upstream version (e.g. an
          # install-script fix at the same tag); a fresh tag naturally resets
          # it. Without this, a revision bumped while fixing a prior release
          # follows the formula forward and produces e.g. `1.5.0_2` when the
          # natural display is `1.5.0`.
          sed -i -E '/^  revision [0-9]+$/d' "$f"
          # Assert the bump actually landed — catches a formula whose url/sha format
          # drifted so a sed matched nothing (better to fail than commit a no-op or
          # a half-updated formula). These assert PRESENCE of the new values only;
          # they cannot see collateral damage elsewhere in the file, which is why
          # the seds above are scoped rather than merely checked afterwards.
          grep -qE "github\.com/${REPO}/archive/refs/tags/${TAG//./\\.}\.tar\.gz" "$f" \
            || { echo "::error::tap bump failed: ${TAG} tarball URL missing in ${f} after sed."; exit 1; }
          grep -q "sha256 \"${NEW_SHA}\"" "$f" \
            || { echo "::error::tap bump failed: sha256 not updated in ${f} after sed."; exit 1; }
          cd tap
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "Formula/${FORMULA}.rb"
          git commit -m "homebrew: bump ${FORMULA} to ${TAG}"
          # Pull-rebase + push with retry: a concurrent tap writer (two app repos
          # releasing at once — the concurrency group above is repo-scoped, so it
          # only serializes runs within one repo) or a manual tap edit makes a
          # plain push non-fast-forward, leaving the release published but the
          # tap stale. On a rebase conflict, abort cleanly and fail loudly rather
          # than leaving the tap mid-rebase; after 3 rejected pushes, fail loud —
          # never fall through green unpushed.
          pushed=0
          for i in 1 2 3; do
            if ! git pull --rebase; then
              git rebase --abort 2>/dev/null || true
              echo "::error::tap rebase hit a conflict — aborted; ${FORMULA} not bumped to ${TAG}. Resolve the tap and re-run."
              exit 1
            fi
            if git push; then pushed=1; break; fi
            echo "push rejected (race); retrying (attempt $i)"
          done
          if [ "$pushed" -ne 1 ]; then
            echo "::error::tap push failed after 3 attempts; ${FORMULA} not bumped to ${TAG}. Re-run this workflow."
            exit 1
          fi
          echo "::notice::Bumped tap to ${TAG} (sha ${NEW_SHA:0:12}…)."
