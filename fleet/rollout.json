{
  "change": "furrow pin v5.0.0 -> v6.0.0 in fleet/task-status.yml (t-2gmm). Unlike the v5.0.0 entry this one carries a board-layout FLAG DAY: v6.0.0 ships schema v10 (repeat, furrow #331, plus the ten follow-ups #333-#342), and the reusable's pre-flight refuses a pinned binary whose schema is not the board's (furrow sync-task-status.yml 'furrow schema mismatch', exit 1), so every caller moved to v6.0.0 reddens on task-status until the shared board is raised with furrow upgrade — canary glyph-test included, for the whole soak, which is the intended shape of that gate and does not touch the read-back evidence (bytes only). The upgrade runs right after the fleet apply, never before it (the reverse order fails every v5.0.0 caller with schema-too-new, exit 3, for the length of the soak). The caller contract moved only in comments between v5.0.0 and v5.1.0 and not at all between v5.1.0 and v6.0.0 (measured: git diff on .github/workflows/sync-task-status.yml is the furrow-version default alone, the lockstep commit furrow #343). The three hub sites bump in this PR: this canonical, the hub's own hand-maintained caller (fleet-sync EXCLUDEs the hub) and the PAT-reminder's checksum-pinned install, whose FURROW_SHA256 is the v6.0.0 linux_amd64 tarball's from the release checksums.txt.",
  "covers": "sha256:3f1e43b52ad85e4d9c0b0419b990c9a736bd3749de925b00b2cd5047c277317f",
  "stage": "canary",
  "canary": [
    "glyph-test"
  ],
  "soak_until": null,
  "evidence": {}
}
