{
  "change": "repo-policy.yml: NEW canonical (t-5sas) — the tool-free house-policy PR gate: (1) tracked *.ja.* paths are banned (doc-consistency-policy; measured 0 hits across 17 fleet repos' origin/main — the perch README.ja.md exists only in a stale local checkout), and (2) an available(macOS …) gate at or below the repo's own Package.swift floor is dead code (measured exactly 1 hit fleet-wide: wand Sources/WandAdapterMacOS/LauncherPanel.swift:1971, #available(macOS 14) under floor 26.0; wand's packaging/icon script is out of scope by design). Logic is scripts/repo-policy-check.sh, table-tested by tests/repo-policy-check.test.sh (14 cases, both directions per check). Canary is wand, per the task: the one repo with a real hit, so the canary shows the gate firing, not just passing. || commit-convention.md: stub rewritten link-driven for glyph v2 (ships as tag v3.0.0; sigil is the version signal, the repo's own glyph.toml is the local truth) — rides the same canary; inert prose, no gate reads it (t-m1x1/t-wk8z).",
  "covers": "sha256:5704c7c36bc31379c63874b27f1b1585bd66e8f74e9d610c0dfaeefd28158e95",
  "stage": "canary",
  "canary": [
    "wand"
  ],
  "soak_until": null,
  "evidence": {}
}
